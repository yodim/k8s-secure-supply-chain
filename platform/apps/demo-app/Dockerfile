# Minimal-CVE base on purpose: a supply chain demo that ships 40 HIGHs in its
# own demo app undermines the point. Chainguard nginx is distroless-style.
FROM cgr.dev/chainguard/nginx:latest

COPY index.html /usr/share/nginx/html/index.html
